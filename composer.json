{
  "name": "gemfind/gemfind-ring-builder",
  "description": "GemFind Ring Builder WordPress plugin (PDF export via Dompdf).",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.1",
    "dompdf/dompdf": "^3.0"
  },
  "config": {
    "sort-packages": true,
    "platform": {
      "php": "8.1.0"
    }
  }
}
